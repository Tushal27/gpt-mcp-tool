# Week 1: unused (local SQLite only).
# Week 2+: Postgres connection string from Supabase, and the shared-secret
# bearer token that remote clients (ChatGPT/Claude) must send.
DATABASE_URL=
MCP_AUTH_TOKEN=
